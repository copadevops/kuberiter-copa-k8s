apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: tomcat
  name: kube2-tomcat9-replicationcontroller
spec:
  replicas: 3
  selector:
    tier: webserver
  template:
    metadata:
      labels:
        tier: webserver
    spec:
      containers:
      - args:
        - HOME
        command:
        - echo
        env:
        - name: testfol
          value: folder
        image: kuberiter/demo:tomcat-v1
        name: tomcat-test
        ports:
        - containerPort: 8088
